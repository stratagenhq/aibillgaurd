export declare function wrapOpenAI<T extends object>(client: T, { key }: {
    key: string;
}): T;
export declare function wrapAnthropic<T extends object>(client: T, { key }: {
    key: string;
}): T;
